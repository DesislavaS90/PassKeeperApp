from django.urls import path

urlpatterns = [
    # path('', MyPasswordList.as_view(), name='password list'),
    # Create will be a button on the top of the list which will redirect to the password_generator.
    # The password_generator will have two buttons - Generate and Save.
    # When Save is hit it will redirect to another form for additional info
    # and after hitting save on the form will going to save it in the DB and show in MypasswordList
    # path('create/', MyPasswordCreate.as_view(), name='password create'),
    # path('<int:pk>/edit', MyPasswordEdit.as_view(), name='password edit'),
    # path('<int:pk>/delete', MyPasswordDelete.as_view(), name='password delete'),

]